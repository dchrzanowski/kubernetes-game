  CASE FILE 002 — "No One Answers"
  ─────────────────────────────────────────────────

  The webapp pods are alive. Running. Waiting.
  But the service routes to nothing. Like the engineers —
  present on the roster, unreachable by phone.

  We found a handwritten note in the server room:
  "Changed the labels. They won't find it now."
  We don't know who wrote it, or what they were hiding.

  The service selector no longer matches the pods.
  Fix it.

  A half dead employee named J(a)son screams:
  """
  -p '{"spec":{"selector":{"app":"webapp"}}}'
  Because some eeejit called the webapp "web-app"!!!!
  """
  And J(a)son passes away with his last words....

  You also find a sticky note that says: kubectl patch svc webapp-svc

  Namespace: level-2

  Commands to begin your investigation:
    kubectl get svc -n level-2
    kubectl get endpoints -n level-2
    kubectl get pods --show-labels -n level-2
    kubectl describe svc webapp-svc -n level-2

  OBJECTIVE: webapp-svc must have at least one ready endpoint.
