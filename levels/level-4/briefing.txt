  CASE FILE 004 — "Bad Blood"
  ─────────────────────────────────────────────────

  This is the last active deployment. The backend.

  Someone pushed a release the night the engineers
  vanished. The image tag is one we don't recognise —
  it doesn't exist in any known registry. The pods
  cannot start. They keep trying.

  Roll it back. Whatever was deployed that night,
  we don't want it running.

  Namespace: level-4

  Commands to begin your investigation:
    kubectl get pods -n level-4
    kubectl rollout history deployment/backend -n level-4
    kubectl rollout undo deployment/backend -n level-4

  OBJECTIVE: All backend pods must reach Running state.
