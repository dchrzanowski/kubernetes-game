Check the logs: kubectl logs <pod-name> -n level-1
The error message will tell you exactly what env var is missing.
Fix it with: kubectl set env deployment/frontend DATABASE_URL=DB_URL_HERE -n level-1
